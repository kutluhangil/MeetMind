-- =============================================
-- seed.sql — Development seed data
-- Run after migrations for local testing.
-- =============================================

-- NOTE: Replace UUIDs with real values from your auth.users table.
-- You can get them by running: SELECT id FROM auth.users;

-- Example: insert a test meeting for manual UI testing
-- INSERT INTO meetings (user_id, title, language, status)
-- VALUES (
--   '<your-user-uuid>',
--   'Q1 Planning Toplantısı',
--   'tr',
--   'completed'
-- );
